[plugin]
name = "lib"
description = "The project's shared library: fixtures, topologies, and helpers reused across proof suites."

# Private dependencies — other plugins this library requires, resolved for this plugin alone (a
# consumer of `lib` never sees them). For example:
#   [plugins]
#   redis = "prova-rs/prova-redis@v1"
[plugins]
